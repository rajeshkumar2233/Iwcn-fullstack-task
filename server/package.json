{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "mocha --exit --recursive tests/",
    "lint": "eslint .",
    "build": "npm run lint && npm test && rm -rf dist && mkdir dist && babel src --out-dir dist --copy-files",
    "predeploy": "npm run build",
    "deploy": "node dist/index.js"
  },
  "author": "rajesh",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mysql2": "^3.2.1",
    "nodemon": "^2.0.22"
  }
}
